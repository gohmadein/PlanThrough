# PlanThrough 第一版产品规则

PlanThrough 是“甘特图上叠加思维导图展开能力”的单画布项目计划工具。横轴为真实时间，纵轴为用户自定义的事件分类。

## 已确认规则

- 创建项目时询问一级节点数量，允许 1—20 个。
- 一级节点允许并行，并允许多个前置节点。
- 父节点时间由用户人工设定。
- 子节点时间必须完全位于父节点时间范围内。
- 同一父节点的兄弟节点时间重叠标记为“并发”。
- 无父子关系的节点时间重叠标记为“跨模块并行”。
- 第一版依赖类型为“完成后开始”和“可并行”。
- 允许同时展开多个节点，最多支持三级节点。
- 时间轴根据可视密度自动在年、季度、月、周、日之间切换。
- 并发区间从横轴向下显示成对虚线边界；橙色表示同父级并发，紫色表示跨模块并行。
- 分类标签固定显示在各分类区域的右侧，避免占用节点起始位置。
- 当前时间线和并发边界线支持悬停说明，并发边界贯穿整个纵向画布。
- 当前时间线和并发边界线的说明框跟随鼠标，并自动避免超出浏览器边缘。
- 项目开始与结束时间在任务图内部使用醒目的端点标记，不占用底部时间轴。
- 暂不实现权限体系。

## 计算规则

- 横向位置和节点宽度只由开始、完成时间计算。
- 相邻时间区间首尾相接不属于并发，只有严格区间相交才属于并发。
- 父节点进度是直接子节点按权重计算的加权平均值。
- 修改有下级的节点状态或进度时，同步更新整棵子任务，然后重新执行加权归并。
- 一级节点使用全局分类；展开后，二级节点在一级节点的半透明框内重新按分类排列，三级节点同理使用嵌套框。
- 关系线只显示同父级兄弟节点，按时间顺序从前一节点右侧中点连接到后一节点左侧中点。
- 时间轴固定显示在画布底部，并跟随横向滚动同步移动。
- 每个展开框底部显示自己的局部时间轴，并相对全局时间轴自动细化一级：年细化为月，月细化为日。
- 拖动修改模式采用草稿机制：拖动实时预览，点击“保存修改”才正式保存，退出模式可放弃本轮修改。
- 拖动节点主体可同时修改时间位置和事件分类；拖动左右边缘分别修改开始时间和完成时间。
- 整体平移带有下级任务的节点时，下级任务随父节点同步平移；缩放父节点时间边界时不得越过已有子节点范围。
- 所有拖动都受项目周期和父节点时间包含性约束；拖动期间兄弟连接线、并发边界、展开框和局部时间轴实时重新计算。
- 项目执行由图内起点显式启动；未启动前所有未完成节点均处于灰色未解锁状态。
- 新建项目的所有节点进度默认均为 0%；点击起点开始执行时，也会将该项目全部节点统一初始化为 0%，避免示例进度干扰顺序解锁。
- 执行顺序按一级节点时间顺序递进，并在每个父节点内部继续按子节点时间顺序递归穿透；同一时刻只有一个叶子任务可修改执行状态和进度。
- 当前叶子任务及其全部祖先节点使用固定黄色虚线边框提醒，不旋转、不流动；完成节点使用绿色状态圆点，未解锁节点使用灰色状态圆点。
- 兄弟节点连接线带箭头；当前节点通往下一兄弟节点时显示流动断线效果，完成瞬间播放电流通过动画，随后转为实线箭头。
- 启动项目或解锁下一任务时，系统自动展开并滚动定位到当前执行路径，同时在右侧打开当前任务。
- 全部叶子任务完成后才解锁终点；点击终点记录项目完成时间，并播放烟花和完成祝贺动画。
- 项目完成后进入可调试状态，保留完成记录与绿色终点，同时允许继续修改任意节点的执行状态和进度。
- 兄弟节点之间不显示箭头；普通状态使用原始层级实线，当前未完成节点通往下一兄弟节点时持续显示黄色断线。
- 前一节点完成时，在对应连线上叠加绿色电流动画，播放约 1.5 秒后自动消失并恢复为普通实线；终点圆点位于最后一个一级节点右上方外侧，向右约 46 像素、向上约 18 像素，文字在圆点正上方居中显示。
- 浏览器本地保存多个项目，新建项目不会覆盖旧项目。
- 节点附件内容保存在当前浏览器的 IndexedDB 文件库，项目数据仅保存文件名、大小等元数据。
- 后代节点的最高风险向父级递归传递。
- 只有节点完成日期早于今天且归并进度小于 100% 时才自动判为红色；已完成节点不触发延误。

## 数据交换

项目自动保存在当前浏览器。用户可以导出 JSON 项目文件并发送给其他人，对方在同一网站中导入后继续编辑。
